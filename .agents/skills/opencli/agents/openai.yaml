interface:
  display_name: "Opencli"
  short_description: "Repo-specific patterns and workflows for opencli"
  default_prompt: "Use the opencli repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true